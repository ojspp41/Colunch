import { style } from '@vanilla-extract/css';

export const checkMessage = style({
  textAlign: 'left',
  marginLeft: '19px',
  marginTop: '5px',
  color: 'red',
});

export const hidden = style({
  display: 'none',
});
